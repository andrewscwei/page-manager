machine:
  node:
    version: 6.1.0
dependencies:
  post:
    - npm run build
deployment:
  release:
    tag: /release-.*/
    commands:
      - chmod +x ./scripts/release.sh
      - ./scripts/release.sh
